import { SignUp } from "@clerk/nextjs";

/**
 * Page component that renders the Clerk SignUp UI.
 *
 * @returns A React element that renders the Clerk `SignUp` component.
 */
export default function SignUpPage() {
  return <SignUp />;
}